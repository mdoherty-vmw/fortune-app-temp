---
  apiVersion: apps/v1
  kind: Deployment
  metadata:
    name: test-app
    namespace: example-fortune
  spec:
    replicas: 2
    selector:
      matchLabels:
        app: test-app
    strategy:
      type: RollingUpdate
      rollingUpdate:
          maxSurge: 25%
          maxUnavailable: 25%
    template:
      metadata:
        name: test-app
        annotations:
          prometheus.io/path: "/actuator/prometheus"
          prometheus.io/port: "8080"
          prometheus.io/scrape: "true"
        labels:
          app: test-app
      spec:
        containers:
        - image: 10.149.88.33:30002/library/example-fortune:82109c0
          name: test-app
          ports:
          - containerPort: 8080
            protocol: TCP
          env:
          - name: SPRING_REDIS_HOST
            value: "redis.redis-ns.svc.cluster.local"
          readinessProbe:
            httpGet:
              path: /all-fortunes.html
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 1
            periodSeconds: 30
